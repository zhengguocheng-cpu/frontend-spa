# 锁定 package-lock.json 版本格式
lockfile-version=3

# 使用淘宝镜像（国内推荐）
registry=https://registry.npmmirror.com/

# 严格模式：确保 package-lock.json 和 package.json 一致
package-lock=true

# 自动安装 peer dependencies
auto-install-peers=true
